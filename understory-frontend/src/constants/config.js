// Shown as a hint on the admin login screen for this demo. The passcode is
// actually enforced server-side (see api/client.js -> adminListUsers), this
// constant is display-only.
export const ADMIN_PASSCODE = "Admin";
