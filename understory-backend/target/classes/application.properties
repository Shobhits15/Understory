# --- MySQL connection ---
# Replace host/port/db name and credentials with your real values.
spring.datasource.url=jdbc:mysql://localhost:3306/understory
spring.datasource.username=root
spring.datasource.password=shobhit15
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Runs schema.sql against the DB on every startup (safe: uses CREATE TABLE IF NOT EXISTS)
spring.sql.init.mode=always
spring.jpa.hibernate.ddl-auto=update

server.port=8088

# Admin passcode for the /api/admin/users endpoint (matches the ADMIN_PASSCODE in the frontend demo)
app.admin.passcode=pro
